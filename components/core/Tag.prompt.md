Squared keyword tag for service categories and filter sets.

```jsx
<Tag selected onClick={() => {}}>Mowing</Tag>
<Tag onRemove={() => {}}>Leaf removal</Tag>
```

Not a status indicator — use Badge for state.