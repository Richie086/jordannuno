Container surface. `flat` for in-app panels, `raised` for marketing cards, `sunken` for grouped detail, `inverse` for dark green feature blocks.

```jsx
<Card variant="raised" accentTop interactive>
  <h3>Seasonal cleanup</h3>
</Card>
```

`accentTop` adds the 3px clay rule used on featured/priority cards.