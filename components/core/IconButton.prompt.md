Icon-only control for dense toolbars and card corners; `label` becomes both aria-label and tooltip title.

```jsx
<IconButton label="Call customer" variant="secondary"><Icon name="phone" /></IconButton>
```

Same variants as Button minus accent. Sizes: 34 / 44 / 52px squares.