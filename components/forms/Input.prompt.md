Single-line field. Always label it; use `hint` for format help and `error` for validation.

```jsx
<Input label="Property address" required placeholder="1420 Elm St" hint="Where the crew should show up" />
```

`prefix`/`suffix` accept icons or units ($, /mo).